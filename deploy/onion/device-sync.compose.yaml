services:
  server:
    environment:
      FACETS_DEVICE_SYNC_ONION_INGRESS_TOKEN: ${FACETS_DEVICE_SYNC_ONION_INGRESS_TOKEN:?Set a separate 32-byte unpadded base64url onion ingress token}
    ports: !reset []
    networks: !override
      - private
      - onion-application

  ingress:
    profiles:
      - direct-disabled-in-onion-mode
    ports: !reset []

  onion-ingress:
    image: caddy:2.10.2-alpine
    depends_on:
      server:
        condition: service_healthy
    environment:
      FACETS_ONION_INGRESS_TOKEN: ${FACETS_DEVICE_SYNC_ONION_INGRESS_TOKEN:?Set a separate 32-byte unpadded base64url onion ingress token}
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /config:size=16m,mode=0755
      - /data:size=16m,mode=0755
    volumes:
      - ./deploy/onion/Caddyfile:/etc/caddy/Caddyfile:ro
      - ${FACETS_DEVICE_SYNC_TLS_DIRECTORY:-./deploy/tls}:/etc/facets-server/tls:ro
      - facets-device-sync-onion-socket:/run/facets-onion
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - DAC_READ_SEARCH
    security_opt:
      - no-new-privileges=true
    networks:
      - onion-application

  tor:
    build:
      context: ./deploy/onion
    depends_on:
      onion-ingress:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "test -s /var/lib/tor/facets-onion/hostname && grep -q 'Bootstrapped 100% (done): Done' /tmp/tor-notices.log"]
      interval: 10s
      timeout: 3s
      retries: 30
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    volumes:
      - facets-device-sync-onion:/var/lib/tor/facets-onion
      - facets-device-sync-onion-socket:/run/facets-onion
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges=true
    networks:
      - tor-egress

networks:
  onion-application:
    internal: true
  tor-egress:

volumes:
  facets-device-sync-onion:
  facets-device-sync-onion-socket:
